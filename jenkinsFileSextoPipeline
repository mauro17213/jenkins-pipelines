pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'hola'
            }
        }

        stage("Tests") {
            steps {
                echo "hola 2.1"
            }
        }

        stage("Deploy") {
            steps {
                sh "docker-compose down -v"
            }
        }
    }
}

